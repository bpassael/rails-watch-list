<h1>New bookmark for <%= @movie.title %></h1>
<%= simple_form_for [@movie, @bookmark],  data: { turbo: false } do |f| %>
  <%= f.input :comment %>
  <%= f.association :list, collection: List.all %>
  <%= f.submit "Submit bookmark", class: "btn btn-primary" %>
<% end %>
